const COLORS = {
    white: '#FFFFFF',
    blueAppColor: '#0043A5',
    screenBackground: '#FAFAFA',
    grayBackground: '#323232',
    borderGray: '#DDDDDD',
    iconGray: '#A9A9A9',
    black: '#000000',
    grayText: '#666666',
    lightGray: '#ECECEC',
    selectedBackground: '#E6EBF4',
    red: '#FF0000'
};

export const backgroundColors = {
    colorStrongGray: COLORS.grayText,
    lightGrayBackground: COLORS.lightGray,
    screenBackground: COLORS.screenBackground,
    white: COLORS.white,
    blueAppColor: COLORS.blueAppColor,
    errorBackground: COLORS.grayBackground,
    selectedBackground: COLORS.selectedBackground,
    red: COLORS.red
};

export const distanceBetween = {
    headerPaddingHorizontal: 20,
    screenPaddingHorizontal: 20,
    formPadding: 20,
    formPaddingVertical: 40
};

export const borderStyles = {
    white: COLORS.white,
    borderColor: COLORS.borderGray,
    colorStrongGray: COLORS.grayText,
    borderAppColor: COLORS.blueAppColor,
    standardWidth: 1,
    boldBorder: 3
};

export const radius = {
    standard: 10,
    button: 30,
    circle: 90
};

export const iconsStyles = {
    small: 14,
    menuIconSize: 22,
    medium: 22,
    bigSize: 28,
    iconSizeNormal: 18,
    white: COLORS.white,
    blueIcon: COLORS.blueAppColor,
    colorNormal: COLORS.iconGray,
    colorBoldGray: COLORS.grayText,
    black: COLORS.black
};

export const fontSizes = {
    extraSmall: 12,
    smallText: 14,
    size16: 16,
    normalText: 18
};

export const textColors = {
    black: COLORS.black,
    white: COLORS.white,
    grayText: COLORS.grayText,
    blueAppColor: COLORS.blueAppColor
};

export const shadows = {
    buttonShadow: {
        shadowColor: COLORS.black,
        shadowOffset: {
            width: 0,
            height: 1,
        },
        shadowOpacity: 0.22,
        shadowRadius: 2.22,
        elevation: 3
    }
};